{application, rexi, [
    {description, "Lightweight RPC server"},
    {vsn, "%version%"},
    {modules, [
        rexi,
        rexi_app,
        rexi_gov_manager,
        rexi_governor,
        rexi_monitor,
        rexi_server,
        rexi_sup,
        rexi_utils
    ]},
    {registered, [
        rexi_sup,
        rexi_server
    ]},
    {applications, [
        kernel,
        stdlib,
        config
    ]},
    {mod, {rexi_app,[]}}
]}.
