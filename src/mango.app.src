{application, mango, [
    {description, "MongoDB API compatibility layer for CouchDB"},
    {vsn, git},
    {registered, []},
    {applications, [
        kernel,
        stdlib,
        config,
        twig,
        fabric
    ]}
]}.
