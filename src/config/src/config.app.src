{application, config, [
    {description, "INI file configuration system for Apache CouchDB"},
    {vsn, "%version%"},
    {modules, [
        config,
        config_app,
        config_listener,
        config_sup,
        config_util,
        config_writer
    ]},
    {registered, [
        config,
        config_event
    ]},
    {applications, [kernel, stdlib]},
    {mod, {config_app, []}}
]}.
